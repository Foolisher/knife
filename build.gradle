apply plugin: 'java'
apply plugin: 'application'

version = '1.0'

mainClassName = "hadoop.MergeTable"

repositories {
    mavenLocal()
    maven {
        url 'http://maven.oschina.net/content/groups/public/'
    }
    maven {
        credentials {
            username 'staff'
            password 'terminus@dd'
        }
        url 'http://repo.aixforce.com/content/repositories/snapshots/'
    }
    maven {
        credentials {
            username 'staff'
            password 'terminus@dd'
        }
        url 'http://repo.aixforce.com/content/repositories/releases/'
    }
    mavenCentral()
}


dependencies {
    compile 'com.codahale.metrics:metrics-core:3.0.2'
    compile 'org.slf4j:slf4j-api:1.7.7'
    compile 'com.google.guava:guava:18.0'
    compile 'org.apache.hadoop:hadoop-hdfs:2.6.0'
    compile 'org.apache.hadoop:hadoop-main:2.6.0'
    compile 'org.apache.hadoop:hadoop-client:2.6.0'
    testCompile group: 'junit', name: 'junit', version: '4.11'
}